{
  "name": "Minimal eBPF example LAB",
  "service": "cli",
  "runArgs": ["--privileged", "--cap-add SYS_ADMIN", "--log-level debug"],
  "remoteUser": "user",
  "shutdownAction": "stopCompose",
  "workspaceFolder": "/workspaces/cli",
  "dockerComposeFile": "compose.yaml",

  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "EditorConfig.EditorConfig"
      ]
    }
  },

  "postCreateCommand": "fish"
}
